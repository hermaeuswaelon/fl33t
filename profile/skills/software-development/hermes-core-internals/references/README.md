# Compression Scheme

This file lives under `hermes-core-internals/references/compression-scheme-one-time-tiers.md`.
It documents the user's explicit compression architecture: 128K parent cap, 32K delegate cap,
3-tier one-time-fire system with 38.2% compression ratios.

Refer to that file before modifying any compression thresholds or tier logic.
